// Compare two string and or characters
var aString = "Hi"
var bString = "hi"
var areStringEqual = aString == bString

// Convert char to string and compare
var aChar = 'H'
var isSameFirstLetter = bString.charAt(0) == aChar // Take 1st char from bString
var isSameCharStr = aChar.toString == "h" // Convert aChar to string to compare

// if - else
if (isSameFirstLetter) print("Nice!")
else print(s"Not the same $bString & $aChar")











